#include <iostream>
#include <cstring>
#include <string>
#include <algorithm>
#include <vector>
#include <iomanip>
typedef long long ll;
using namespace std;

int main(){
    ll N,M; cin >> N >> M;
    vector<ll> a(M);
    vector<ll> b(M);
    vector<ll> c(M-1);
    for(ll i = 0; i < M; i++){
        cin >> a[i];
        cin >> b[i];
        if(i != M-1) c[i] == i+2;
    }

    do{
        
    }
}