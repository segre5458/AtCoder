#include <iostream>
#include <cstring>
#include <string>
#include <algorithm>
#include <vector>
#include <iomanip>
#include <stack>
#include <cstdlib>
#include <queue>
typedef long long ll;
using namespace std;

int main(){
    ll N,Q; cin >> N >> Q;
    ll cnt = 0;
    for(ll i = 0; i < Q; i++){
        ll a; cin >> a;
        if(a == 1){
            bool OK = true;
            ll b,c; cin >> b >> c;
            for(ll j = 0; j < cnt; j++){
                if()
            }
        }
        else if(a == 2)
        else if(a == 3)
    }
}